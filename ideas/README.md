# ideas/ — parked concepts

Nothing here is part of the current scope (see docs/ROADMAP.md). Files are
kept so no intuition is lost. Each carries: the idea, why it's parked, and the
 trigger condition to un-park it. If a parked idea starts blocking the main
 ladder, it belongs here, not in docs/.
