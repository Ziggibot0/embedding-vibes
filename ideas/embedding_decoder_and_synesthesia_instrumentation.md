# Decoding embeddings back to text; synesthesia measurement rig (parked — exploratory)

## Idea (a) Decoder
Learn an embedding→text decoder to make A-space "legible." User's own
assessment: "a BITCH." Not needed for navigation: Impl 3 steers TO nearby
logged exemplars (reroll/re-prompt), never decodes.

## Idea (b) The synesthesia program (the real reason this file exists)
User reports synesthetic "entropic visions of flattened semantic space" — a
subjective claim about the low-dimensional structure of trained
representations: meaning occupies a thin manifold; failure/recovery/loop have
recognizable shapes on it. Rather than dismiss or literally believe it, treat
it as a hypothesis generator with instrumentation:
  - intrinsic-dimension estimates along trajectories (success vs failure);
  - "flattening" = local id/dispersion collapse — measurable;
  - topological descriptors (loop closure for circular reasoning — exp3
    data exists to test the literal claim);
  - personal qualitative reports become pre-registered predictions checked
    against these metrics (user generates; probes adjudicate).

## Why parked
Not on the critical path; publishable only as a legitimate
interpretability/qualitative-methods appendix AFTER the main results exist.
Park with affection — it is the reason the project exists.

## Un-park trigger
P1/P2 deliver the A-space object; then "flatten/loop/vibe" claims become
testable against measured geometry instead of metaphor.
